{% extends 'base.html.twig' %}

{% block title %}New Events{% endblock %}

{% block body %}
    <h1>Create new Events</h1>
    <div class="container">
    {{ include('events/_form.html.twig') }}

    <a href="{{ path('events_index') }}"><button type="button" class="btn btn-primary">back to list</button></a>
    </div>
{% endblock %}
