{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
    <h1>Edit Events</h1>
    <div class="container">

    {{ include('events/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('events_index') }}"><button type="button" class="btn btn-outline-primary mb-3">back to list</button></a>

    {{ include('events/_delete_form.html.twig') }}
    </div>
{% endblock %}
